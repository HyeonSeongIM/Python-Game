#배경이미지 불러오기 (괄호 안에 png 설정해서 img 넣기)
background = pygame.image.load()

while 문 안에
screen.fill((0, 0, 255)) # 색 채워넣기

# screen.blit(background, (0, 0)) # 배경 그리기
    
pygame.display.update() # 반드시 계속해서 화면 그리기 필요

